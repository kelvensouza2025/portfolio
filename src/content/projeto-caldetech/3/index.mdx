# Diagrama da Arquitetura

## Diagrama de deploy

```mermaid
flowchart TD

  %% Front-end
  subgraph Frontend
    FEWeb["Web App"]
    FEMobile["Mobile App"]
  end

  %% Back-end
  subgraph Backend
    API["API"]
    DB["PostgreSQL Database"]
    S3["AWS S3 (Armazenamento de Imagens)"]
  end

  %% Infraestrutura
  subgraph Infraestrutura
    Vercel["Vercel (Hospedagem Front-end)"]
    Beanstalk["AWS Elastic Beanstalk (Hospedagem Back-end)"]
  end

  %% Conexões
  FEWeb -->|API REST| API
  FEMobile -->|API REST| API

  API -->|Prisma ORM| DB
  API -->|Uploads| S3

  FEWeb --> Vercel
  API --> Beanstalk
```
